#!/bin/sh -e

mk() {
    make \
        PREFIX=/usr/local \
        ZLIB=no \
        CC="${CC:-gcc}" \
        OPT="$CFLAGS" \
        SHAREDIR=/usr/share/hwdata \
        MANDIR=/usr/share/man \
        SBINDIR=/usr/local/bin \
        "$@"
}

mk all
mk DESTDIR="$1" install install-lib
