#!/bin/sh -e

./configure  \
    --prefix=/usr \
    --sysconfdir=/etc \
    --without-php \
    --disable-avahi \
    --disable-mutool \
    --disable-dbus \
    --disable-braille \
    --without-rcdir 
	
make
make DESTDIR="$1" install
