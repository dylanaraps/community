#!/bin/sh -e

./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --localstatedir=/var \
    --without-all \
    --without-x \
    --with-x-toolkit=no \
    --with-x=no \
    --with-xml2=yes

make DESTDIR="$1" install
