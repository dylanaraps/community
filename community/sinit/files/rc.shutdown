#!/bin/sh

case "$1" in
    reboot|poweroff)
    ;;
    *)
        echo "Invalid action '$1' for rc.shutdown" 1>&2
        exit 1
        ;;
esac

. /lib/init/rc.shutdown

case "$1" in
    reboot)
        ubase-box halt -r
        ;;
    poweroff)
        ubase-box halt -p
        ;;
esac
