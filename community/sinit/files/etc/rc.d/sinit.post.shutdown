if [ "$SINIT_ENABLE_SHUTDOWN_HOOKS" = 1 ]; then
    case "$1" in
        reboot)
            ubase-box halt -r
            ;;
        poweroff)
            ubase-box halt -p
            ;;
    esac
fi
