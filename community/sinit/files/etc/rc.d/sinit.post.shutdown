sinit_post_shutdown () {
    case "$1" in
        reboot)
            ubase-box halt -r
            ;;
        poweroff)
            ubase-box halt -p
            ;;
    esac
}

[ "$SINIT_ENABLE_SHUTDOWN_HOOKS" = 1 ] && sinit_post_shutdown
