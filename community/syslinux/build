#!/bin/sh -e

unset CFLAGS LDFLAGS

for f in *.patch ; do patch -p1 < "$f"; done

: > mk/devel.mk

make clean

make bios efi64 installer

make -j1 bios efi64 MANDIR=/usr/share/man INSTALLROOT="$1" install
