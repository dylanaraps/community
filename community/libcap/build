#!/bin/sh -e

sed -i "s/CFLAGS :=/CFLAGS := \$(CFLAGS)/;/LDFLAGS :=/d" Make.Rules

patch -p1 < use_sched_not_pthread.patch

make
make \
    PREFIX=/usr \
    SBINDIR=/usr/bin \
    LIBDIR=/usr/lib \
    DESTDIR="$1" \
    RAISE_SETFCAP=no \
    install
