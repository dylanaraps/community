#!/bin/sh -e

sed -i "s/CFLAGS :=/CFLAGS += \$(CPPFLAGS) /" Make.Rules
sed -i "s/LDFLAGS :=/LDFLAGS +=/" Make.Rules

make
make \
    PREFIX=/usr \
    SBINDIR=/usr/bin \
    LIBDIR=/usr/lib \
    DESTDIR="$1" \
    RAISE_SETFCAP=no \
    install
